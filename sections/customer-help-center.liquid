{%- liquid
  assign help_section_id = 'hc-' | append: section.id
-%}

<help-center-section class="block">
  <div
    class="relative overflow-hidden py-16 md:py-24"
    style="background-color: {{ section.settings.bg_color | default: '#f8f6f1' }}; color: #0c0c0b;"
  >
    <div class="absolute inset-x-0 top-0 h-24" style="background: linear-gradient(180deg, rgba(255,255,255,0.68), transparent);"></div>

    <div class="relative mx-auto max-w-3xl px-5 md:px-8">
      <div class="mb-10 md:mb-12">
        <p
          class="ui-pill mb-4"
          style="--ui-fg: #6f6558;"
        >
          Help center
        </p>
        <h2 class="font-heading text-[clamp(2.3rem,6vw,4rem)] leading-[0.95] tracking-[-0.05em]" style="color: #0c0c0b;">
          {{ section.settings.heading | default: 'Need a hand?' }}
        </h2>
        <p class="mt-4 max-w-2xl font-body text-[0.98rem] leading-7 md:text-[1.02rem]" style="color: #645c50;">
          {{ section.settings.description | default: "Find clear answers about shipping, returns, payments, and order support. If you still need help, reach out and we'll point you to the right next step." }}
        </p>
      </div>

      <div
        class="ui-surface ui-surface--panel overflow-hidden"
      >
        {%- if section.blocks.size > 0 -%}
          {%- for block in section.blocks -%}
            {%- if block.type == 'category_header' -%}
              <div class="border-b px-5 py-4 md:px-7 md:py-5" style="border-color: rgba(12,12,11,0.06); background-color: #f8f4ee;" {{ block.shopify_attributes }}>
                <p class="ui-pill" style="--ui-fg: #6f6558; --ui-bg: rgba(255,255,255,0.45); --ui-shadow: none; --ui-blur: 0px; letter-spacing: 0.28em; min-height: 1.6rem; padding-inline: 0.55rem;">
                  {{ block.settings.title | default: 'General' }}
                </p>
              </div>
            {%- elsif block.type == 'faq_item' and block.settings.question != blank -%}
              {%- assign faq_uid = help_section_id | append: '-' | append: forloop.index0 -%}
              <div class="hc-faq-item border-b last:border-b-0" style="border-color: rgba(12,12,11,0.06);" data-open="false" {{ block.shopify_attributes }}>
                <button
                  type="button"
                  id="{{ faq_uid }}-button"
                  aria-expanded="false"
                  aria-controls="{{ faq_uid }}-panel"
                  class="hc-faq-trigger flex w-full items-center gap-4 px-5 py-5 text-left transition-colors duration-300 md:px-7 md:py-6"
                  data-open="false"
                  data-faq-trigger
                >
                  <span class="min-w-0 flex-1">
                    <span class="mb-1 block text-[10px] font-semibold uppercase tracking-[0.24em]" style="color: rgba(111,101,88,0.78);">Question</span>
                    <span class="block font-heading text-[1.03rem] leading-[1.45] tracking-[-0.03em] md:text-[1.08rem]" style="color: #0c0c0b;">
                      {{ block.settings.question }}
                    </span>
                  </span>

                  <span
                    class="hc-faq-icon ui-icon-disc ui-icon-disc--sm shrink-0"
                    style="--ui-bg: #f4efe7; --ui-border: rgba(12,12,11,0.08); --ui-shadow: none;"
                    aria-hidden="true"
                  >
                    <svg class="hc-icon hc-icon--plus block h-4 w-4" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.9" aria-hidden="true">
                      <line x1="8" y1="3" x2="8" y2="13"></line>
                      <line x1="3" y1="8" x2="13" y2="8"></line>
                    </svg>
                    <svg class="hc-icon hc-icon--close hidden h-4 w-4" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.9" aria-hidden="true">
                      <line x1="4" y1="4" x2="12" y2="12"></line>
                      <line x1="12" y1="4" x2="4" y2="12"></line>
                    </svg>
                  </span>
                </button>

                <div
                  id="{{ faq_uid }}-panel"
                  role="region"
                  aria-labelledby="{{ faq_uid }}-button"
                  aria-hidden="true"
                  class="hc-faq-panel"
                  data-open="false"
                >
                  <div class="px-5 pb-5 md:px-7 md:pb-6">
                    <p class="max-w-2xl font-body text-[0.94rem] leading-7" style="color: #5f564b;">
                      {{ block.settings.answer | default: 'We keep the answer brief here so the page stays easy to scan.' }}
                    </p>
                  </div>
                </div>
              </div>
            {%- endif -%}
          {%- endfor -%}
        {%- else -%}
          {%- for i in (1..8) -%}
            {%- assign q_key = 'q' | append: i -%}
            {%- assign a_key = 'a' | append: i -%}
            {%- assign question = section.settings[q_key] -%}
            {%- assign answer = section.settings[a_key] -%}
            {%- if question != blank -%}
              {%- assign faq_uid = help_section_id | append: '-legacy-' | append: i -%}
              <div class="hc-faq-item border-b last:border-b-0" style="border-color: rgba(12,12,11,0.06);" data-open="false">
                <button
                  type="button"
                  id="{{ faq_uid }}-button"
                  aria-expanded="false"
                  aria-controls="{{ faq_uid }}-panel"
                  class="hc-faq-trigger flex w-full items-center gap-4 px-5 py-5 text-left transition-colors duration-300 md:px-7 md:py-6"
                  data-open="false"
                  data-faq-trigger
                >
                  <span class="min-w-0 flex-1">
                    <span class="mb-1 block text-[10px] font-semibold uppercase tracking-[0.24em]" style="color: rgba(111,101,88,0.78);">Question</span>
                    <span class="block font-heading text-[1.03rem] leading-[1.45] tracking-[-0.03em] md:text-[1.08rem]" style="color: #0c0c0b;">
                      {{ question }}
                    </span>
                  </span>

                  <span
                    class="hc-faq-icon ui-icon-disc ui-icon-disc--sm shrink-0"
                    style="--ui-bg: #f4efe7; --ui-border: rgba(12,12,11,0.08); --ui-shadow: none;"
                    aria-hidden="true"
                  >
                    <svg class="hc-icon hc-icon--plus block h-4 w-4" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.9" aria-hidden="true">
                      <line x1="8" y1="3" x2="8" y2="13"></line>
                      <line x1="3" y1="8" x2="13" y2="8"></line>
                    </svg>
                    <svg class="hc-icon hc-icon--close hidden h-4 w-4" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.9" aria-hidden="true">
                      <line x1="4" y1="4" x2="12" y2="12"></line>
                      <line x1="12" y1="4" x2="4" y2="12"></line>
                    </svg>
                  </span>
                </button>

                <div
                  id="{{ faq_uid }}-panel"
                  role="region"
                  aria-labelledby="{{ faq_uid }}-button"
                  aria-hidden="true"
                  class="hc-faq-panel"
                  data-open="false"
                >
                  <div class="px-5 pb-5 md:px-7 md:pb-6">
                    <p class="max-w-2xl font-body text-[0.94rem] leading-7" style="color: #5f564b;">
                      {{ answer | default: 'We keep the answer brief here so the page stays easy to scan.' }}
                    </p>
                  </div>
                </div>
              </div>
            {%- endif -%}
          {%- endfor -%}
        {%- endif -%}
      </div>
    </div>
  </div>
</help-center-section>

<script>
  if (!customElements.get('help-center-section')) {
    class HelpCenterSection extends HTMLElement {
      connectedCallback() {
        this._reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
        this._triggers = [...this.querySelectorAll('[data-faq-trigger]')];
        this._handlers = new Map();
        this._resizeHandler = () => this.updateOpenPanels();

        this._triggers.forEach((trigger) => {
          const handler = () => this.toggle(trigger);
          this._handlers.set(trigger, handler);
          trigger.addEventListener('click', handler);
        });

        window.addEventListener('resize', this._resizeHandler, { passive: true });
      }

      disconnectedCallback() {
        this._triggers?.forEach((trigger) => {
          const handler = this._handlers?.get(trigger);
          if (handler) trigger.removeEventListener('click', handler);
        });

        window.removeEventListener('resize', this._resizeHandler);
        this._triggers = [];
        this._handlers = new Map();
        this._resizeHandler = null;
      }

      toggle(trigger) {
        const item = trigger.closest('.hc-faq-item');
        if (!item) return;

        const isOpen = item.dataset.open === 'true';
        this.closeAll();

        if (!isOpen) {
          this.openItem(trigger);
        }
      }

      openItem(trigger) {
        const item = trigger.closest('.hc-faq-item');
        const panel = this.getPanel(trigger);

        if (!item || !panel) return;

        item.dataset.open = 'true';
        trigger.dataset.open = 'true';
        trigger.setAttribute('aria-expanded', 'true');
        panel.dataset.open = 'true';
        panel.setAttribute('aria-hidden', 'false');

        panel.style.maxHeight = `${panel.scrollHeight}px`;
        if (this._reduceMotion) {
          panel.style.transition = 'none';
        }
      }

      closeAll() {
        this._triggers.forEach((trigger) => {
          const item = trigger.closest('.hc-faq-item');
          const panel = this.getPanel(trigger);

          if (item) {
            item.dataset.open = 'false';
          }

          trigger.dataset.open = 'false';
          trigger.setAttribute('aria-expanded', 'false');

          if (panel) {
            panel.dataset.open = 'false';
            panel.setAttribute('aria-hidden', 'true');
            panel.style.maxHeight = '0px';
            if (this._reduceMotion) {
              panel.style.transition = 'none';
            }
          }
        });
      }

      updateOpenPanels() {
        this._triggers.forEach((trigger) => {
          const item = trigger.closest('.hc-faq-item');
          const panel = this.getPanel(trigger);
          if (!item || !panel || item.dataset.open !== 'true') return;
          panel.style.maxHeight = `${panel.scrollHeight}px`;
        });
      }

      getPanel(trigger) {
        const panelId = trigger.getAttribute('aria-controls');
        if (!panelId) return null;

        return this.querySelector(`#${CSS.escape(panelId)}`);
      }
    }

    customElements.define('help-center-section', HelpCenterSection);
  }
</script>

{% schema %}
{
  "name": "Customer Help Center",
  "tag": "section",
  "class": "help-center-section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Need a hand?"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Find clear answers about shipping, returns, payments, and order support. If you still need help, reach out and we'll point you to the right next step."
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#f8f6f1"
    },
    {
      "type": "header",
      "content": "Legacy FAQ (used when no blocks exist)"
    },
    {
      "type": "header",
      "content": "Question 1"
    },
    {
      "type": "text",
      "id": "q1",
      "label": "Question 1",
      "default": "What is your return policy?"
    },
    {
      "type": "textarea",
      "id": "a1",
      "label": "Answer 1",
      "default": "We accept returns within 30 days of delivery for unworn items in original condition."
    },
    {
      "type": "header",
      "content": "Question 2"
    },
    {
      "type": "text",
      "id": "q2",
      "label": "Question 2",
      "default": "What if my order arrives damaged?"
    },
    {
      "type": "textarea",
      "id": "a2",
      "label": "Answer 2",
      "default": "Contact us within 48 hours with photos and we'll review the issue and guide the next step."
    },
    {
      "type": "header",
      "content": "Question 3"
    },
    {
      "type": "text",
      "id": "q3",
      "label": "Question 3",
      "default": "How do I track my order?"
    },
    {
      "type": "textarea",
      "id": "a3",
      "label": "Answer 3",
      "default": "When your order ships, we'll email a tracking link you can use at any time."
    },
    {
      "type": "header",
      "content": "Question 4"
    },
    {
      "type": "text",
      "id": "q4",
      "label": "Question 4",
      "default": "Which payment methods do you accept?"
    },
    {
      "type": "textarea",
      "id": "a4",
      "label": "Answer 4",
      "default": "We accept major credit cards, PayPal, Apple Pay, Google Pay, and Shop Pay."
    },
    {
      "type": "header",
      "content": "Question 5"
    },
    {
      "type": "text",
      "id": "q5",
      "label": "Question 5",
      "default": "Can I edit or cancel my order?"
    },
    {
      "type": "textarea",
      "id": "a5",
      "label": "Answer 5",
      "default": "If you need a change, contact us within 1 hour of placing the order."
    },
    {
      "type": "header",
      "content": "Question 6"
    },
    {
      "type": "text",
      "id": "q6",
      "label": "Question 6"
    },
    {
      "type": "textarea",
      "id": "a6",
      "label": "Answer 6"
    },
    {
      "type": "header",
      "content": "Question 7"
    },
    {
      "type": "text",
      "id": "q7",
      "label": "Question 7"
    },
    {
      "type": "textarea",
      "id": "a7",
      "label": "Answer 7"
    },
    {
      "type": "header",
      "content": "Question 8"
    },
    {
      "type": "text",
      "id": "q8",
      "label": "Question 8"
    },
    {
      "type": "textarea",
      "id": "a8",
      "label": "Answer 8"
    }
  ],
  "blocks": [
    {
      "type": "category_header",
      "name": "Category Header",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Category Title",
          "default": "Shipping and delivery"
        }
      ]
    },
    {
      "type": "faq_item",
      "name": "FAQ Item",
      "settings": [
        {
          "type": "text",
          "id": "question",
          "label": "Question",
          "default": "What is your return policy?"
        },
        {
          "type": "textarea",
          "id": "answer",
          "label": "Answer",
          "default": "We accept returns within 30 days of delivery for unworn items in original condition."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Customer Help Center",
      "blocks": [
        {
          "type": "faq_item",
          "settings": {
            "question": "What is your return policy?",
            "answer": "We accept returns within 30 days of delivery for unworn items in original condition."
          }
        },
        {
          "type": "faq_item",
          "settings": {
            "question": "What if my order arrives damaged?",
            "answer": "Contact us within 48 hours with photos and we'll review the issue and guide the next step."
          }
        },
        {
          "type": "faq_item",
          "settings": {
            "question": "How do I track my order?",
            "answer": "When your order ships, we'll email a tracking link you can use at any time."
          }
        }
      ]
    }
  ]
}
{% endschema %}

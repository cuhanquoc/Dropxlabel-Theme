<help-center-section class="block">
  <!-- ── Section with white bg and generous padding ── -->
  <div style="background-color: {{ section.settings.bg_color | default: '#ffffff' }}; padding-top: 3.5rem; padding-bottom: 3.5rem;" class="md:py-20">
    <div class="max-w-[720px] mx-auto px-5 md:px-8">

      <!-- ── Heading ── -->
      <h2 style="font-size: clamp(2rem, 6vw, 3.2rem); font-weight: 800; line-height: 1.1; color: #0a0a0a; margin-bottom: 1rem; font-family: var(--font-heading, 'Playfair Display', serif);">
        {{ section.settings.heading | default: 'Customer Help Center' }}
      </h2>
      <p style="font-size: 0.92rem; line-height: 1.75; color: #666; margin-bottom: 2.5rem; max-width: 600px; font-family: var(--font-body, 'Inter', sans-serif);">
        {{ section.settings.description | default: "Welcome to our Customer Help Center! We're here to assist you with any questions or concerns you may have. Whether you need help with orders, shipping, returns, or product details, you'll find all the answers right here." }}
      </p>

      <!-- ── FAQ Items ── -->
      <div class="hc-faq-list" style="overflow: hidden;">

        {%- if section.blocks.size > 0 -%}
          {%- comment -%} Block-based rendering — merchant adds FAQ items via Theme Editor {%- endcomment -%}
          {%- for block in section.blocks -%}
            {%- if block.type == "category_header" -%}
              <div class="hc-category-header" {{ block.shopify_attributes }} style="padding: 2rem 0 0.5rem; border-bottom: 2px solid #0a0a0a; margin-bottom: 0.5rem;">
                <h3 style="font-family: var(--font-heading, 'Playfair Display', serif); font-size: 0.75rem; font-weight: 700; letter-spacing: 0.1em; text-transform: uppercase; color: #0a0a0a;">
                  {{ block.settings.title }}
                </h3>
              </div>

            {%- elsif block.type == "faq_item" -%}
              {%- if block.settings.question != blank -%}
                <div class="hc-faq-item" {{ block.shopify_attributes }} style="border-top: 1px solid #e5e5e5; transition: background-color 0.4s ease, border-radius 0.4s ease;">
                  <button class="hc-faq-btn" aria-expanded="false"
                          style="width: 100%; display: flex; align-items: center; justify-content: space-between; padding: 1.25rem 0.5rem; gap: 1rem; text-align: left; background: none; border: none; cursor: pointer;">
                    <span class="hc-faq-question" style="font-family: var(--font-heading, 'Playfair Display', serif); font-weight: 700; font-size: 1.1rem; line-height: 1.3; color: #0a0a0a; transition: color 0.3s ease;">
                      {{ block.settings.question }}
                    </span>
                    <span class="hc-faq-icon" style="flex-shrink: 0; width: 36px; height: 36px; border-radius: 50%; background-color: #0a0a0a; display: flex; align-items: center; justify-content: center; transition: background-color 0.3s ease;">
                      <svg class="hc-icon-plus" width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="white" stroke-width="2" style="transition: transform 0.3s ease;">
                        <line x1="8" y1="3" x2="8" y2="13"/>
                        <line x1="3" y1="8" x2="13" y2="8"/>
                      </svg>
                      <svg class="hc-icon-close" width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="white" stroke-width="2" style="display: none; transition: transform 0.3s ease;">
                        <line x1="4" y1="4" x2="12" y2="12"/>
                        <line x1="12" y1="4" x2="4" y2="12"/>
                      </svg>
                    </span>
                  </button>
                  <div class="hc-faq-answer" style="max-height: 0; overflow: hidden; opacity: 0; transition: max-height 0.5s cubic-bezier(0.4, 0, 0.2, 1), opacity 0.4s ease;">
                    <div style="padding: 0 0.5rem 1.25rem 0.5rem;">
                      <p style="font-family: var(--font-body, 'Inter', sans-serif); font-size: 0.88rem; line-height: 1.75; color: rgba(255,255,255,0.75); transition: color 0.3s ease;">
                        {{ block.settings.answer }}
                      </p>
                    </div>
                  </div>
                </div>
              {%- endif -%}
            {%- endif -%}
          {%- endfor -%}

        {%- else -%}
          {%- comment -%} Legacy fallback — settings q1..q8 / a1..a8 {%- endcomment -%}
          {%- for i in (1..8) -%}
            {%- assign q_key = 'q' | append: i -%}
            {%- assign a_key = 'a' | append: i -%}
            {%- assign question = section.settings[q_key] -%}
            {%- assign answer = section.settings[a_key] -%}
            {%- if question != blank -%}
              <div class="hc-faq-item" style="border-top: 1px solid #e5e5e5; transition: background-color 0.4s ease, border-radius 0.4s ease;">

                <button class="hc-faq-btn" aria-expanded="false"
                        style="width: 100%; display: flex; align-items: center; justify-content: space-between; padding: 1.25rem 0.5rem; gap: 1rem; text-align: left; background: none; border: none; cursor: pointer;">
                  <span class="hc-faq-question" style="font-family: var(--font-heading, 'Playfair Display', serif); font-weight: 700; font-size: 1.1rem; line-height: 1.3; color: #0a0a0a; transition: color 0.3s ease;">
                    {{ question }}
                  </span>
                  <span class="hc-faq-icon" style="flex-shrink: 0; width: 36px; height: 36px; border-radius: 50%; background-color: #0a0a0a; display: flex; align-items: center; justify-content: center; transition: background-color 0.3s ease;">
                    <!-- Plus icon -->
                    <svg class="hc-icon-plus" width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="white" stroke-width="2" style="transition: transform 0.3s ease;">
                      <line x1="8" y1="3" x2="8" y2="13"/>
                      <line x1="3" y1="8" x2="13" y2="8"/>
                    </svg>
                    <!-- Close icon -->
                    <svg class="hc-icon-close" width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="white" stroke-width="2" style="display: none; transition: transform 0.3s ease;">
                      <line x1="4" y1="4" x2="12" y2="12"/>
                      <line x1="12" y1="4" x2="4" y2="12"/>
                    </svg>
                  </span>
                </button>

                <div class="hc-faq-answer" style="max-height: 0; overflow: hidden; opacity: 0; transition: max-height 0.5s cubic-bezier(0.4, 0, 0.2, 1), opacity 0.4s ease;">
                  <div style="padding: 0 0.5rem 1.25rem 0.5rem;">
                    <p style="font-family: var(--font-body, 'Inter', sans-serif); font-size: 0.88rem; line-height: 1.75; color: rgba(255,255,255,0.75); transition: color 0.3s ease;">
                      {{ answer }}
                    </p>
                  </div>
                </div>

              </div>
            {%- endif -%}
          {%- endfor -%}
        {%- endif -%}

      </div>

    </div>
  </div>
</help-center-section>

<script>
  if (!customElements.get('help-center-section')) {
    class HelpCenterSection extends HTMLElement {
      constructor() {
        super();
        this._buttons = [];
      }

      connectedCallback() {
        this._buttons = [...this.querySelectorAll('.hc-faq-btn')];
        this._buttons.forEach(btn => {
          btn._handler = () => this.toggle(btn);
          btn.addEventListener('click', btn._handler);
        });
      }

      disconnectedCallback() {
        this._buttons.forEach(btn => {
          btn.removeEventListener('click', btn._handler);
        });
        this._buttons = [];
      }

      toggle(btn) {
        const item = btn.closest('.hc-faq-item');
        const answer = item.querySelector('.hc-faq-answer');
        const question = btn.querySelector('.hc-faq-question');
        const icon = btn.querySelector('.hc-faq-icon');
        const iconPlus = btn.querySelector('.hc-icon-plus');
        const iconClose = btn.querySelector('.hc-icon-close');
        const isOpen = btn.getAttribute('aria-expanded') === 'true';

        /* Spin the icon circle */
        icon.style.transition = 'background-color 0.3s ease, transform 0.4s cubic-bezier(0.16, 1, 0.3, 1)';
        icon.style.transform = 'rotate(360deg)';
        setTimeout(() => { icon.style.transform = ''; }, 450);

        if (isOpen) {
          /* ── Close ── */
          answer.style.maxHeight = '0';
          answer.style.opacity = '0';
          item.style.backgroundColor = 'transparent';
          item.style.borderRadius = '0';
          item.style.margin = '0';
          question.style.color = '#0a0a0a';
          icon.style.backgroundColor = '#0a0a0a';
          iconPlus.style.display = '';
          iconClose.style.display = 'none';
          btn.setAttribute('aria-expanded', 'false');
        } else {
          /* ── Close all others first ── */
          this._buttons.forEach(otherBtn => {
            if (otherBtn !== btn && otherBtn.getAttribute('aria-expanded') === 'true') {
              const otherItem = otherBtn.closest('.hc-faq-item');
              const otherAnswer = otherItem.querySelector('.hc-faq-answer');
              const otherQuestion = otherBtn.querySelector('.hc-faq-question');
              const otherIcon = otherBtn.querySelector('.hc-faq-icon');
              const otherPlus = otherBtn.querySelector('.hc-icon-plus');
              const otherClose = otherBtn.querySelector('.hc-icon-close');
              otherAnswer.style.maxHeight = '0';
              otherAnswer.style.opacity = '0';
              otherItem.style.backgroundColor = 'transparent';
              otherItem.style.borderRadius = '0';
              otherItem.style.margin = '0';
              otherQuestion.style.color = '#0a0a0a';
              otherIcon.style.backgroundColor = '#0a0a0a';
              otherPlus.style.display = '';
              otherClose.style.display = 'none';
              otherBtn.setAttribute('aria-expanded', 'false');
            }
          });

          /* ── Open ── */
          item.style.backgroundColor = '#0a0a0a';
          item.style.borderRadius = '12px';
          item.style.margin = '0 -0.5rem';
          item.style.padding = '0 0.5rem';
          question.style.color = '#ffffff';
          icon.style.backgroundColor = '#333333';
          iconPlus.style.display = 'none';
          iconClose.style.display = '';
          answer.style.maxHeight = answer.scrollHeight + 'px';
          answer.style.opacity = '1';
          btn.setAttribute('aria-expanded', 'true');
        }
      }
    }

    customElements.define('help-center-section', HelpCenterSection);
  }
</script>

{% schema %}
{
  "name": "Customer Help Center",
  "tag": "section",
  "class": "help-center-section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Customer Help Center"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Welcome to our Customer Help Center! We're here to assist you with any questions or concerns you may have. Whether you need help with orders, shipping, returns, or product details, you'll find all the answers right here."
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#ffffff"
    },
    {
      "type": "header",
      "content": "Legacy FAQ (used when no blocks exist)"
    },
    {
      "type": "header",
      "content": "Question 1"
    },
    {
      "type": "text",
      "id": "q1",
      "label": "Question 1",
      "default": "What is your return and exchange policy?"
    },
    {
      "type": "textarea",
      "id": "a1",
      "label": "Answer 1",
      "default": "We offer a hassle-free return and exchange policy within 30 days of delivery. Items must be unused, in original packaging, and with tags intact. For exchanges, simply initiate a request through our return portal or contact support."
    },
    {
      "type": "header",
      "content": "Question 2"
    },
    {
      "type": "text",
      "id": "q2",
      "label": "Question 2",
      "default": "What if I received a damaged or incorrect item?"
    },
    {
      "type": "textarea",
      "id": "a2",
      "label": "Answer 2",
      "default": "If you receive a damaged or incorrect item, please contact our support team within 48 hours of delivery with photos of the issue. We'll arrange a replacement or full refund as quickly as possible."
    },
    {
      "type": "header",
      "content": "Question 3"
    },
    {
      "type": "text",
      "id": "q3",
      "label": "Question 3",
      "default": "How can I track my order?"
    },
    {
      "type": "textarea",
      "id": "a3",
      "label": "Answer 3",
      "default": "Once your order is shipped, you'll receive a confirmation email with a tracking number. You can use this number on our website or the carrier's site to track your package in real time."
    },
    {
      "type": "header",
      "content": "Question 4"
    },
    {
      "type": "text",
      "id": "q4",
      "label": "Question 4",
      "default": "What payment methods do you accept?"
    },
    {
      "type": "textarea",
      "id": "a4",
      "label": "Answer 4",
      "default": "We accept all major credit cards (Visa, MasterCard, American Express), PayPal, Apple Pay, Google Pay, and Shop Pay. All transactions are secured with SSL encryption."
    },
    {
      "type": "header",
      "content": "Question 5"
    },
    {
      "type": "text",
      "id": "q5",
      "label": "Question 5",
      "default": "Can I modify or cancel my order after placing it?"
    },
    {
      "type": "textarea",
      "id": "a5",
      "label": "Answer 5",
      "default": "You can modify or cancel your order within 1 hour of placing it by contacting our support team. After that window, orders enter processing and cannot be changed."
    },
    {
      "type": "header",
      "content": "Question 6"
    },
    {
      "type": "text",
      "id": "q6",
      "label": "Question 6"
    },
    {
      "type": "textarea",
      "id": "a6",
      "label": "Answer 6"
    },
    {
      "type": "header",
      "content": "Question 7"
    },
    {
      "type": "text",
      "id": "q7",
      "label": "Question 7"
    },
    {
      "type": "textarea",
      "id": "a7",
      "label": "Answer 7"
    },
    {
      "type": "header",
      "content": "Question 8"
    },
    {
      "type": "text",
      "id": "q8",
      "label": "Question 8"
    },
    {
      "type": "textarea",
      "id": "a8",
      "label": "Answer 8"
    }
  ],
  "blocks": [
    {
      "type": "category_header",
      "name": "Category Header",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Category Title",
          "default": "Shipping & Delivery"
        }
      ]
    },
    {
      "type": "faq_item",
      "name": "FAQ Item",
      "settings": [
        {
          "type": "text",
          "id": "question",
          "label": "Question",
          "default": "What is your return policy?"
        },
        {
          "type": "textarea",
          "id": "answer",
          "label": "Answer",
          "default": "We offer a hassle-free 30-day return policy."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Customer Help Center",
      "blocks": [
        {
          "type": "faq_item",
          "settings": {
            "question": "What is your return and exchange policy?",
            "answer": "We offer a hassle-free return and exchange policy within 30 days of delivery. Items must be unused, in original packaging, and with tags intact."
          }
        },
        {
          "type": "faq_item",
          "settings": {
            "question": "What if I received a damaged or incorrect item?",
            "answer": "If you receive a damaged or incorrect item, please contact our support team within 48 hours of delivery with photos of the issue."
          }
        },
        {
          "type": "faq_item",
          "settings": {
            "question": "How can I track my order?",
            "answer": "Once your order is shipped, you'll receive a confirmation email with a tracking number."
          }
        }
      ]
    }
  ]
}
{% endschema %}
